<Window x:Class="BatchSheetRenamer.BatchSheetRenamerForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Batch Sheet Renamer"
        Width="980"
        Height="640"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize"
        Style="{StaticResource WindowBase}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles/BrandTokens.xaml" />
                <ResourceDictionary Source="Styles/Controls.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="8"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Prefix" Style="{StaticResource LabelBase}" Margin="0,0,8,8"/>
                <TextBox Grid.Row="0" Grid.Column="1" Style="{StaticResource InputBase}" Text="{Binding Prefix, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="0" Grid.Column="3" Text="Suffix" Style="{StaticResource LabelBase}" Margin="0,0,8,8"/>
                <TextBox Grid.Row="0" Grid.Column="4" Style="{StaticResource InputBase}" Text="{Binding Suffix, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                <Button Grid.Row="0" Grid.Column="6" Content="Preview" Style="{StaticResource SecondaryButton}" Width="120" Height="32" Margin="0,0,0,8" Click="Preview_Click"/>
                <Button Grid.Row="0" Grid.Column="8" Content="Selecteer alles" Style="{StaticResource SecondaryButton}" Width="120" Height="32" Margin="0,0,0,8" Click="SelectAll_Click"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Find" Style="{StaticResource LabelBase}" Margin="0,0,8,0"/>
                <TextBox Grid.Row="1" Grid.Column="1" Style="{StaticResource InputBase}" Text="{Binding FindText, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="1" Grid.Column="3" Text="Replace" Style="{StaticResource LabelBase}" Margin="0,0,8,0"/>
                <TextBox Grid.Row="1" Grid.Column="4" Style="{StaticResource InputBase}" Text="{Binding ReplaceText, UpdateSourceTrigger=PropertyChanged}"/>

                <Button Grid.Row="1" Grid.Column="8" Content="Selecteer niets" Style="{StaticResource SecondaryButton}" Width="120" Height="32" Click="SelectNone_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="0">
            <DataGrid ItemsSource="{Binding Items}"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0"
                      Margin="0">
                <DataGrid.Columns>
                    <DataGridCheckBoxColumn Binding="{Binding Selected, UpdateSourceTrigger=PropertyChanged}" Header="Apply" Width="70"/>
                    <DataGridTextColumn Binding="{Binding SheetNumber}" Header="Sheet #" Width="120" IsReadOnly="True"/>
                    <DataGridTextColumn Binding="{Binding OriginalName}" Header="Huidige naam" Width="*" IsReadOnly="True"/>
                    <DataGridTextColumn Binding="{Binding PreviewName}" Header="Nieuwe naam (preview)" Width="*" IsReadOnly="True"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Controleer preview en klik Uitvoeren om de geselecteerde sheets te hernoemen."
                       VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Muted}"/>

            <Button Grid.Column="1" Content="Uitvoeren" Style="{StaticResource PrimaryButton}" Width="120" Height="32" Click="Apply_Click"/>
            <Button Grid.Column="3" Content="Annuleren" Style="{StaticResource SecondaryButton}" Width="120" Height="32" Click="Cancel_Click"/>
        </Grid>
    </Grid>
</Window>
